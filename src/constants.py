"""src/constants.py — Shared constant definitions for word boundaries and probability fallbacks."""

# Symbol used to mark the beginning and end of a token
WORD_BOUNDARY = "#"

# Log‑probability fallback for unseen grams
MIN_LOG_PROB = float("-inf")

# End of src/constants.py